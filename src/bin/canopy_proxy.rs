//! Placeholder. Real implementation lands in the next commit (the canopy-proxy
//! sidecar binary: loopback S3P proxy with a broker-backed credential
//! provider).

fn main() {
	eprintln!("canopy-proxy: not yet implemented");
	std::process::exit(1);
}
